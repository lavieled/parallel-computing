# HW0 — C++ essentials

Practice with lambdas, functors, and a sequential skip list.

## Files

- `lambda.cpp` — `init_globals()`, `ascending_sort()`
- `cdf.h` — `Accumulator`, `cdf()`
- `skip_list_seq_impl.h` — `SkipListImpl`
- `lambda.h`, `functor.h`, `skip_list_seq.h` — provided headers

## Build (smoke)

```bash
g++ -std=c++11 -c lambda.cpp
```
